{
  "first_divergent_frame": -13,
  "checked_frames": 110,
  "export_version": "v10",
  "updated": "2026-09-13",
  "note": "Re-measured against published pack v10 (/mnt/archive/datasets/melee/skirmish-gameplay/v10-snapshot-20260913/fox-fd/match-data.bin, sha256 e4189b85; the specials' per-frame script command variables are now embedded). Previous baseline (v9, 106 frames, first divergence -17, P2's action_age expected 5.0 actual 4.0 mid-SpecialNLoop) is superseded: 110 frames now match (-123 through -14). The v9 baseline's own divergence (P2's action_age, mid-SpecialNLoop) is now fixed, and by the same fix as fox-fd-2.slp's own baseline (docs/validation.md's projectile-owner-hitlag entry), not a script/cmd_vars timing bug in characters::fox::neutral as originally suspected: a direct probe confirms P2's own action_state/action_age now matches the recording bit-exactly on every single frame from -123 through -3, ruling out any Loop re-entry/cmd_vars-reset timing issue (checked directly per the coordinator's own hint: BeginLoopTransition/FinishLoopTransition's re-entry does not need the extra ftAnim_8006EBA4 advance the Start-entry family does -- confirmed against the pinned decomp, ftfoxspecialn.c:316-333, no such call there -- and P2's own action_frame progression bears this out exactly, already correct before this loop touched anything in neutral.rs). The fix instead was P2's own action_age freezing after ITS OWN laser connected (the identical attacker-hitlag bug fox-fd-2.slp exposed), which was masking a same-shape divergence in both recordings. With that fixed, checked_frames/first_divergent_frame stay at 110/-13, but the field changes: P4's action_state (expected 0x002b/43 = LandingFallSpecial, actual 0x004e/78 = Damage, grounded/middle/level-0 hitstun). P4 is continuously in LandingFallSpecial from -19 onward (age climbing past 18 by -13); the recording shows P4's own percent still increasing (3 at -13, 6 at -5, matching P2's laser connecting again) while P4 stays in LandingFallSpecial throughout, uninterrupted -- Skirmish instead transitions P4 into ordinary grounded hitstun (Damage/78) the instant the hit lands, same as any other hit. This looks like a genuinely unmodeled interruption rule (a hit landing during LandingFallSpecial's own lag not always forcing a hitstun-state transition in decomp, unlike Skirmish's unconditional transition), not the loop-timing or projectile-ordering shape this loop was assigned; reported per this loop's own stop condition rather than guessed at further."
}

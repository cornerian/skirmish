{
  "first_divergent_frame": -13,
  "checked_frames": 110,
  "export_version": "v10",
  "updated": "2026-09-13",
  "note": "Re-measured against published pack v10 (/mnt/archive/datasets/melee/skirmish-gameplay/v10-snapshot-20260913/fox-fd/match-data.bin, sha256 e4189b85; the specials' per-frame script command variables are now embedded, so the Blaster's arming and fire timing come from the script instead of this port's older approximation -- docs/parity.md's own 'Published pack v10' note). Previous baseline (v9, 106 frames, first divergence -17, P2's action_age expected 5.0 actual 4.0 mid-SpecialNLoop) is superseded: 110 frames now match (-123 through -14); the new divergence is still P2's action_age, still mid-SpecialNLoop (state 342), now at frame -13 (expected 9.0, actual 8.0) -- the same one-frame-lag shape as the v9 baseline's own divergence, just later in the loop's own per-shot cycle, consistent with the script data changing when each cycle's own action_frame resets but not fixing the underlying one-frame lag. Not diagnosed further this loop: the loop-cycle timing this shape points at (Action::SpecialNLoop's own script-driven re-arm/fire cadence) lives entirely in src/game/characters/fox/neutral.rs, one of the two files (with src/game/projectile.rs) a concurrent Falco Blaster batch is already reserved on per this loop's own coordination instructions; skipped and reported rather than risking a collision with that batch's own in-flight edits."
}

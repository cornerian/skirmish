{
  "first_divergent_frame": -5,
  "checked_frames": 118,
  "export_version": "v10",
  "updated": "2026-09-13",
  "note": "Re-measured against published pack v10 (/mnt/archive/datasets/melee/skirmish-gameplay/v10-snapshot-20260913/fox-fd/match-data.bin, sha256 e4189b85; the specials' per-frame script command variables are now embedded, so the Blaster's arming and fire timing come from the script instead of this port's older approximation -- docs/parity.md's own 'Published pack v10' note). Previous baseline (v8, 114 frames, first divergence -9) is superseded: this loop did not diagnose the -9 divergence itself (undocumented in docs/parity.md/validation.md, and not reproduced -- the combination of pack v10 and the many Skirmish fixes landed on origin/main since the v8 baseline was recorded already reaches past it). 118 frames now match (-123 through -6); the new divergence at -5 is P1's last_attack_landed (expected 0x12/18, Melee's Blaster/neutral-special attack id, actual 0x00). validate-replay's own report stops at this field (the first mismatch in port-then-field order, P1 before P2), which reads as 'the hit never lands or is never recorded'; a direct, uncommitted probe stepping the native match frame-by-frame (bypassing the CLI's stop-on-first-difference report to dump both ports' full observations) shows the truer shape: P2's own `percent` also mismatches at -5 (expected 3.0, actual 0.0 -- Skirmish has not applied the hit yet at all), but by -4 both `last_attack_landed` (18) and `percent` (3.0) already match the recording. This is a plain one-frame lag in when Skirmish's laser bolt registers its hit, not a missing hit or a missing bookkeeping write: `game::projectile::step` (`src/game/projectile.rs`) moves each projectile and checks its swept hurtbox contact the same frame it is dispatched from `characters::fox::neutral::drain_pending_shot` (`src/game/simulation.rs`'s own comment: 'matching the source's own same-frame item Anim/Phys/Coll'), so the lag traces to exactly when a shot is queued to fire relative to the Loop subaction script's own per-frame `cmd_vars` (`characters::fox::neutral`'s script-driven arming/fire check) -- Fox/Falco neutral-special (Blaster) territory. Not chased further: both `src/game/characters/fox/neutral.rs` and `src/game/projectile.rs` are reserved by a concurrent Falco Blaster batch per this loop's own coordination instructions; skipped and reported rather than risking a collision with that batch's in-flight edits."
}

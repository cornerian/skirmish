# skirmish-equivalence

Semantic traces, process comparison, native match adapters, and differential
probe executables. Test fixtures remain under `tests/support`; these reusable
equivalence tools are intentionally outside the main simulation library.

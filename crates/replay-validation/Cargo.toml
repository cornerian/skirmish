[package]
name = "replay-validation"
version = "0.1.0"
edition = "2024"
description = "Streaming checkpoint-based frame validation for future replay adapters"
publish = false

[dependencies]
thiserror = "2"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"

[package]
name = "melee-input"
version = "0.1.0"
edition = "2024"
description = "Exact Dolphin controller clamp routines for headless Melee consumers"
publish = false

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"

[package]
name = "skirmish-replay"
version = "0.1.0"
edition = "2024"
description = "Streaming checkpoint-based frame validation for future replay adapters"
publish = false

[dependencies]
thiserror = "2"

[dev-dependencies]
melee-physics = { path = "../melee-physics" }
melee-runtime = { path = "../melee-runtime" }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
